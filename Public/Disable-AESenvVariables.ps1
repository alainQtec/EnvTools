function Disable-AESenvVariables {
    <#
    .SYNOPSIS
        Disable AES encrypted Environment Variables
    .DESCRIPTION
        Configures/deactivates AES encryption in Environment Variables
        ie: Once run, all cmdlets of EnvTools PsModule will stop using AES when seting environment variables.
    .NOTES
        Information or caveats about the function e.g. 'This function is not supported in Linux'
    .LINK
        Specify a URI to a help page, this will show when Get-Help -Online is used.
    .LINK
        Initialize-EnvConfig
    .LINK
        Disable-AESenvVariables
    .EXAMPLE
        Test-MyTestFunction -Verbose
        Explanation of the function or its result. You can include multiple examples with additional .EXAMPLE lines
    #>
    [CmdletBinding()]
    param (

    )

    begin {
    }

    process {
    }

    end {
    }
}